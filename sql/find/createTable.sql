CREATE TABLE IF NOT EXISTS `find` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `text` varchar(255) NOT NULL,
  PRIMARY KEY (`id`)
);

INSERT INTO `find` (`text`) VALUES
  ('Quedar y ver que surge'),
  ('Hablar y conocer gente nueva'),
  ('Busco una relación');
